<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="@dimen/height_mt_item_oneline">

  <FrameLayout
    android:id="@+id/bookmark_active_marker"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/mapotempo_green"
    android:alpha="0.2"
    android:visibility="invisible"/>

  <ImageView
    android:id="@+id/iv__bookmark_color"
    android:layout_marginLeft="@dimen/margin_base"
    android:padding="2dp"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"/>

  <TextView
      android:id="@+id/tv__bookmark_name"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_centerVertical="true"
      android:layout_marginLeft="@dimen/margin_base"
      android:layout_toLeftOf="@+id/iv__bookmark_drag"
      android:layout_toRightOf="@id/iv__bookmark_color"
      android:singleLine="true"
      android:textAppearance="@style/MwmTextAppearance.Body1"
      android:text="empty"/>

  <ImageView
    android:id="@+id/iv__bookmark_drag"
    android:layout_width="36dp"
    android:layout_height="36dp"
    android:layout_alignParentRight="true"
    android:layout_centerVertical="true"
    android:layout_marginRight="@dimen/margin_base"
    android:src="@drawable/mt_drag"
    android:background="@android:color/transparent"/>

</RelativeLayout>
